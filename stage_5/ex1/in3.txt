decl

int swap(int *p, int *q);
int sum(int*a,int*b);
enddecl

int sum(int*a,int*b)
{
	decl
		int o;
	enddecl
	begin
		o=*a+*b;
		return o;
	end
}
int swap(int *p, int *q)
{
decl
int temp; 
enddecl

begin
    *p=*p+*q;
    *q=*p-*q;
    *p=*p-*q;
    
    return 0;
end

}


int main()
{
decl
int x;
int y;
int io;
int result;
enddecl

begin
    write("p & f test");
    

    write("Enter X :");
    read(x);
    write("Enter Y:");
    read(y);
    write("Swap:Enter 1");
    write("Sum:Enter 2");
    read(io);
    if(io==1) then
    write("Values BEFORE swap:");
    write(x);
    write(y);
  
    result = swap(&x, &y);

    write("Values AFTER swap:");
    write(x);
    write(y);
  else
  	result=sum(&x,&y);
  	write(result);
  endif;
    return 0;
end

} 
